services:
  leisaac:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leisaac-base
    stdin_open: true
    tty: true
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
      - DISPLAY=${DISPLAY}
      - ACCEPT_EULA=Y
      - PRIVACY_CONSENT=Y
      - OMNI_KIT_ALLOW_ROOT=1
    volumes:
      # X11 forwarding for GUI
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ${HOME}/.Xauthority:/root/.Xauthority:rw
      # Mount your project scripts
      - ./scripts:/workspace/project/scripts
      # IsaacSim caches (separate from 3-cam container)
      - leisaac-base-cache-kit:/root/.cache/kit
      - leisaac-base-cache-ov:/root/.cache/ov
      - leisaac-base-cache-pip:/root/.cache/pip
      - leisaac-base-cache-gl:/root/.cache/nvidia/GLCache
      - leisaac-base-cache-compute:/root/.nv/ComputeCache
      - leisaac-base-logs:/root/.nvidia-omniverse/logs
      - leisaac-base-data:/root/.local/share/ov/data
      # Shared memory for IsaacSim
      - /dev/shm:/dev/shm
      - ../assets:/workspace/leisaac/assets
    network_mode: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
volumes:
  leisaac-base-cache-kit:
  leisaac-base-cache-ov:
  leisaac-base-cache-pip:
  leisaac-base-cache-gl:
  leisaac-base-cache-compute:
  leisaac-base-logs:
  leisaac-base-data: